import{a as o,c,b as d,o as t}from"./app-CLHFgJWN.js";const a={};function n(i,e){return t(),c("div",null,e[0]||(e[0]=[d('<h2 id="countdownlatch" tabindex="-1"><a class="header-anchor" href="#countdownlatch"><span>CountDownLatch</span></a></h2><h3 id="countdownlatch-的底层实现原理是什么-你用它做过什么功能" tabindex="-1"><a class="header-anchor" href="#countdownlatch-的底层实现原理是什么-你用它做过什么功能"><span>CountDownLatch 的底层实现原理是什么？你用它做过什么功能？</span></a></h3><p><code>CountDownLatch</code> 是 Java 并发包 (<code>java.util.concurrent</code>) 中的一个同步工具类，它允许一个或多个线程等待其他一组线程完成操作。</p><h4 id="底层实现原理" tabindex="-1"><a class="header-anchor" href="#底层实现原理"><span>底层实现原理</span></a></h4><p><code>CountDownLatch</code> 的底层实现依赖于 <code>AbstractQueuedSynchronizer</code> (AQS)，这是一个用于构建锁和同步器的框架。<code>CountDownLatch</code> 内部有一个继承了 AQS 的同步器 <code>Sync</code>。</p><p>其核心原理如下：</p><ol><li><p><strong>初始化状态</strong>：在创建 <code>CountDownLatch</code> 对象时，会传入一个整型参数 <code>count</code>，这个值会被设置为 AQS 内部的 <code>state</code> 变量。这个 <code>state</code> 代表了需要等待完成的操作数量。</p></li><li><p><strong><code>await()</code> 方法</strong>：</p><ul><li><p>当一个线程调用 <code>await()</code> 方法时，它会检查当前的 <code>state</code> 值。</p></li><li><p>如果 <code>state</code> 的值为 <code>0</code>，表示所有需要等待的操作已经完成，<code>await()</code> 方法会立即返回。</p></li><li><p>如果 <code>state</code> 的值不为 <code>0</code>，该线程会被封装成一个 <code>Node</code> 节点，并加入到 AQS 的同步等待队列中，然后线程被挂起 (park)，进入休眠状态，等待被唤醒。</p></li></ul></li><li><p><strong><code>countDown()</code> 方法</strong>：</p><ul><li><p>当一个任务完成时，它会调用 <code>countDown()</code> 方法。</p></li><li><p>此方法会以线程安全的方式（通过 CAS 操作）将 <code>state</code> 的值减 <code>1</code>。</p></li><li><p>当 <code>state</code> 的值被减到 <code>0</code> 时，意味着所有等待的事件都已发生。此时，<code>countDown()</code> 方法会唤醒 (unpark) 所有在同步队列中等待的线程。</p></li></ul></li></ol><p>总结来说，<code>CountDownLatch</code> 通过 AQS 的 <code>state</code> 变量来作为计数器。<code>await()</code> 操作依赖于 <code>state</code> 是否为 <code>0</code> 来决定是否阻塞线程，而 <code>countDown()</code> 操作则递减 <code>state</code>，并在 <code>state</code> 归零时唤醒所有等待的线程。</p><h4 id="应用场景" tabindex="-1"><a class="header-anchor" href="#应用场景"><span>应用场景</span></a></h4><p>我曾在一个数据初始化场景中使用过 <code>CountDownLatch</code>。系统启动时，需要并行地从多个不同的数据源（如数据库、缓存、第三方 API）加载初始化数据。主线程必须等待所有数据都加载完毕后，才能继续执行后续的业务逻辑（例如，对外提供服务）。</p><p>具体实现如下：</p><ol><li><p>创建一个 <code>CountDownLatch</code>，其计数器的初始值设置为数据源的数量，例如 <code>new CountDownLatch(3)</code>。</p></li><li><p>为每个数据加载任务创建一个独立的线程，并在任务的 <code>run()</code> 方法中执行数据加载逻辑。</p></li><li><p>在每个数据加载线程的 <code>finally</code> 块中，调用 <code>latch.countDown()</code> 方法，确保无论加载成功与否，计数器都会减一。</p></li><li><p>主线程在启动所有数据加载线程后，立即调用 <code>latch.await()</code> 方法。此时，主线程会阻塞，直到计数器变为 <code>0</code>。</p></li><li><p>当所有数据加载任务都执行完毕并调用了 <code>countDown()</code> 后，计数器归零，主线程从 <code>await()</code> 方法中被唤醒，继续执行后续的启动流程。</p></li></ol><p>通过这种方式，<code>CountDownLatch</code> 优雅地实现了主线程对多个并行任务完成状态的等待，提升了系统的启动效率。</p>',13)]))}const p=o(a,[["render",n]]),r=JSON.parse('{"path":"/notes/interview/we3gifnt/","title":"JUC","lang":"zh-CN","frontmatter":{"title":"JUC","createTime":"2025/07/24 22:37:54","permalink":"/notes/interview/we3gifnt/"},"readingTime":{"minutes":2.54,"words":761},"git":{"createdTime":1753699516000,"updatedTime":1753699516000,"contributors":[{"name":"dead_summer","username":"","email":"2941325451@qq.com","commits":1,"avatar":"https://gravatar.com/avatar/d7f172441d823f01ad688f425860dbe76a31ea11c9936176bbd8c14670a619f8?d=retro"}]},"filePathRelative":"notes/八股文自救指南/Java/JUC.md","headers":[]}');export{p as comp,r as data};
