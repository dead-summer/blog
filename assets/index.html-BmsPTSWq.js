import{a as s,c as i,b as t,o}from"./app-CLHFgJWN.js";const d={};function l(n,e){return o(),i("div",null,e[0]||(e[0]=[t(`<h2 id="b-树索引" tabindex="-1"><a class="header-anchor" href="#b-树索引"><span>B+ 树索引</span></a></h2><p>索引的核心目标是减少磁盘 I/O 次数以提升查询效率。MySQL 的 InnoDB 存储引擎以<strong>页 (Page)</strong> 为磁盘 I/O 单位（默认 16KB）。B+ 树的节点大小与页对齐，使得树高度直接影响 I/O 次数。</p><h3 id="为什么使用-b-树作为索引的数据结构" tabindex="-1"><a class="header-anchor" href="#为什么使用-b-树作为索引的数据结构"><span>为什么使用 B+ 树作为索引的数据结构？</span></a></h3><p>通过对比其他数据结构说明 B+ 树的优势：</p><table><thead><tr><th>数据结构</th><th>优点</th><th>缺点</th></tr></thead><tbody><tr><td><strong>哈希表</strong></td><td>等值查询时间复杂度 O(1)</td><td>不支持范围查询，退化为全表扫描</td></tr><tr><td><strong>二叉查找树</strong></td><td>理想情况下查询效率较高</td><td>有序插入时退化为链表，树高剧增，导致磁盘 I/O 次数过多</td></tr><tr><td><strong>B 树</strong></td><td>多路平衡，树高低于二叉树</td><td>非叶子节点存储数据记录，减少索引键容量，限制分支因子</td></tr><tr><td><strong>B+ 树</strong></td><td><strong>查询稳定、范围查询高效、树高最低</strong></td><td>无主要缺陷</td></tr></tbody></table><p>B+ 树的三大核心特性：</p><ol><li><strong>非叶子节点仅存储索引键</strong>：显著提升分支因子（单个节点容纳更多键），减少树高和磁盘 I/O。</li><li><strong>数据记录仅存于叶子节点</strong>：保证查询路径长度固定（从根节点到叶子节点）。</li><li><strong>叶子节点形成有序双向链表</strong>：支持高效范围遍历。</li></ol><h3 id="b-树和-b-树的区别" tabindex="-1"><a class="header-anchor" href="#b-树和-b-树的区别"><span>B 树和 B+ 树的区别</span></a></h3><table><thead><tr><th>特性</th><th>B 树</th><th>B+ 树</th></tr></thead><tbody><tr><td><strong>数据存储位置</strong></td><td>所有节点存储索引键 + 数据记录</td><td>非叶子节点仅存索引键；数据记录存于叶子节点</td></tr><tr><td><strong>分支因子</strong></td><td>较低（数据记录占用空间）</td><td>极高（无数据记录占用）</td></tr><tr><td><strong>范围查询效率</strong></td><td>低（需中序遍历，跨节点访问）</td><td>极高（叶子节点链表直接遍历）</td></tr><tr><td><strong>查询稳定性</strong></td><td>不稳定（数据可能在非叶子节点找到）</td><td>稳定（必须访问叶子节点）</td></tr></tbody></table><h3 id="什么是聚集索引和二级索引" tabindex="-1"><a class="header-anchor" href="#什么是聚集索引和二级索引"><span>什么是聚集索引和二级索引？</span></a></h3><p>在 InnoDB 存储引擎中，索引主要分为聚集索引 (Clustered Index) 和二级索引 (Secondary Index)。</p><ol><li><p><strong>聚集索引 (Clustered Index)</strong></p><ul><li><strong>定义</strong>: 叶子节点存储完整行数据，物理存储顺序与索引逻辑顺序一致。</li><li><strong>结构</strong>: 每张表<strong>有且仅有</strong>一个聚集索引。</li><li><strong>选择规则</strong>: <ol><li>如果表定义了 <code>PRIMARY KEY</code>，则主键索引就是聚集索引。</li><li>如果没有 <code>PRIMARY KEY</code>，InnoDB 会选择第一个<strong>不包含 NULL 值</strong>的 <code>UNIQUE</code> 索引作为聚集索引。</li><li>如果以上两者都没有，InnoDB 会隐式地创建一个名为 <code>GEN_CLUST_INDEX</code> 的 6 字节隐藏主键作为聚集索引。</li></ol></li><li><strong>叶子节点存储内容</strong>: <code>主键值</code> + <code>完整的行记录</code>。</li></ul></li><li><p><strong>二级索引 (Secondary Index)</strong></p><ul><li><strong>定义</strong>: 二级索引，也称为非聚集索引。它的叶子节点不存储完整的行数据，而是存储<strong>索引键的值</strong>和对应记录的<strong>主键值</strong>。</li><li><strong>结构</strong>: 一张表可以拥有多个二级索引。</li><li><strong>叶子节点存储内容</strong>: <code>索引列的值</code> + <code>主键值</code>。</li></ul></li></ol><h3 id="什么是回表和索引覆盖" tabindex="-1"><a class="header-anchor" href="#什么是回表和索引覆盖"><span>什么是回表和索引覆盖？</span></a></h3><p>这两个概念都与二级索引的使用密切相关。</p><ol><li><p><strong>回表 (Table Access by Index RowID)</strong></p><ul><li><strong>定义</strong>: 使用二级索引查询时，若所需列未完全覆盖，需用主键值回查聚集索引获取完整数据。这个<strong>再次查询聚集索引</strong>的过程，就称为“回表”。</li><li><strong>示例</strong>:<div class="language-sql line-numbers-mode" data-highlighter="shiki" data-ext="sql" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-sql"><span class="line"><span style="--shiki-light:#A0ADA0;--shiki-dark:#758575DD;">-- age 列上存在二级索引</span></span>
<span class="line"><span style="--shiki-light:#1E754F;--shiki-dark:#4D9375;">SELECT</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> id, age, </span><span style="--shiki-light:#1E754F;--shiki-dark:#4D9375;">name</span><span style="--shiki-light:#1E754F;--shiki-dark:#4D9375;"> FROM</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> student </span><span style="--shiki-light:#1E754F;--shiki-dark:#4D9375;">WHERE</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> age </span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">=</span><span style="--shiki-light:#2F798A;--shiki-dark:#4C9A91;"> 20</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;">;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div></li><li><strong>执行过程</strong>: <ol><li>在 <code>age</code> 索引树中搜索 <code>age = 20</code> 的记录，找到对应的叶子节点。</li><li>从叶子节点中获取该记录的<strong>主键值</strong> (例如 <code>id = 101</code>)。</li><li>使用获取到的主键值 <code>101</code>，回到<strong>主键索引 (聚集索引)</strong> 中进行查找。</li><li>在主键索引的叶子节点中找到 <code>id = 101</code> 的完整行记录，并从中读取 <code>id</code>, <code>age</code>, <code>name</code> 字段返回。</li></ol></li><li><strong>影响</strong>: 回表操作需要进行两次索引查找，增加了额外的磁盘 I/O 和查询开销，应尽量避免。</li></ul></li><li><p><strong>索引覆盖 (Covering Index)</strong></p><ul><li><strong>定义</strong>: 当一个查询语句所需要获取的所有列数据，都恰好能从一个二级索引的叶子节点中直接获取时，就不再需要进行回表操作。这种情况就称为“索引覆盖”。</li><li><strong>示例</strong>:<div class="language-sql line-numbers-mode" data-highlighter="shiki" data-ext="sql" style="--shiki-light:#393a34;--shiki-dark:#dbd7caee;--shiki-light-bg:#ffffff;--shiki-dark-bg:#121212;"><pre class="shiki shiki-themes vitesse-light vitesse-dark vp-code"><code class="language-sql"><span class="line"><span style="--shiki-light:#A0ADA0;--shiki-dark:#758575DD;">-- age 列上存在二级索引</span></span>
<span class="line"><span style="--shiki-light:#1E754F;--shiki-dark:#4D9375;">SELECT</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> id, age </span><span style="--shiki-light:#1E754F;--shiki-dark:#4D9375;">FROM</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> student </span><span style="--shiki-light:#1E754F;--shiki-dark:#4D9375;">WHERE</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;"> age </span><span style="--shiki-light:#AB5959;--shiki-dark:#CB7676;">=</span><span style="--shiki-light:#2F798A;--shiki-dark:#4C9A91;"> 20</span><span style="--shiki-light:#393A34;--shiki-dark:#DBD7CAEE;">;</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div></li><li><strong>执行过程</strong>: <ol><li>在 <code>age</code> 索引树中搜索 <code>age = 20</code> 的记录，找到对应的叶子节点。</li><li>二级索引的叶子节点本身就存储了 <code>age</code> 和 <code>主键 id</code> 的值。查询所需的所有列 (<code>id</code>, <code>age</code>) 均已获取。</li><li>直接返回结果，无需回表查询主键索引。</li></ol></li><li><strong>优势</strong>: 索引覆盖是一种高效的查询优化策略，它减少了查询主键索引的步骤，避免了回表带来的性能损耗，显著提升了查询效率。</li></ul></li></ol><h2 id="索引失效" tabindex="-1"><a class="header-anchor" href="#索引失效"><span>索引失效</span></a></h2><p>在 MySQL 中，如果查询优化器 (Optimizer) 判断使用索引的成本高于全表扫描，或者 <code>WHERE</code> 子句的用法不满足特定规则，就会导致索引失效，转而执行全表扫描。</p><h3 id="常见的索引失效场景有哪些" tabindex="-1"><a class="header-anchor" href="#常见的索引失效场景有哪些"><span>常见的索引失效场景有哪些？</span></a></h3><ol><li><p><strong>在索引列上进行计算、函数或类型转换</strong></p><ul><li><strong>描述</strong>: 对索引列进行任何形式的计算、函数调用（包括隐式类型转换）都会导致索引失效。因为索引保存的是原始值，对列进行操作后，索引树将无法直接用于查找。</li><li><strong>索引失效</strong>： <ul><li>索引使用表达式计算：<code>SELECT * FROM user WHERE age + 10 = 30;</code></li><li>索引使用函数：<code>select * from t_user where length(name)=6;</code></li><li>索引蕴含隐式类型转换: <code>SELECT * FROM user WHERE phone = 18812345678;</code> MySQL 会将字符串类型的 <code>phone</code> 字段转换为数字类型进行比较，相当于 <code>CAST(phone AS signed)</code>，这属于函数操作，导致索引失效。</li></ul></li></ul></li><li><p><strong>违反联合索引的最左前缀法则 (Leftmost Prefix Rule)</strong></p><ul><li><strong>描述</strong>: 当创建一个联合索引，如 <code>INDEX(col1, col2, col3)</code> 时，查询必须从索引的最左侧列开始，并且不能跳过中间的列。</li><li><strong>原理</strong>: 联合索引的 B-Tree 结构是按照索引定义中的列顺序进行排序的。首先按 <code>col1</code> 排序，<code>col1</code> 相同时再按 <code>col2</code> 排序，以此类推。如果查询条件没有 <code>col1</code>，优化器将无法利用这个 B-Tree 结构来定位数据。</li><li><strong>示例</strong>: 对于 <code>INDEX(a, b, c)</code>： <ul><li><p><strong>有效查询</strong>:</p><ul><li>使用 <code>a</code>：<code>WHERE a = 1;</code></li><li>使用 <code>a, b</code>：<code>WHERE a = 1 AND b = 2;</code></li><li>使用 <code>a, b, c</code>：<code>WHERE a = 1 AND b = 2 AND c = 3;</code></li><li>仅使用 <code>a</code>，因为 <code>b</code> 被跳过：<code>WHERE a = 1 AND c = 3;</code></li></ul></li><li><p><strong>失效查询</strong>:</p><ul><li>缺少最左侧的 <code>a</code>：<code>WHERE b = 2;</code></li><li>缺少最左侧的 <code>a</code> 和 <code>b</code>：<code>WHERE c = 3;</code></li><li>缺少最左侧的 <code>a</code>：<code>WHERE b = 2 AND c = 3;</code></li></ul></li></ul></li></ul></li><li><p><strong>使用 <code>LIKE</code> 查询时以通配符开头</strong></p><ul><li><strong>描述</strong>: <code>LIKE</code> 查询如果以 <code>%</code> 或 <code>_</code> 作为开头，索引将无法被利用。</li><li><strong>原理</strong>: B-Tree 索引是有序的，必须知道前缀才能进行范围查找。未知的前缀使得数据库无法定位到索引树中的起始点。</li><li><strong>示例</strong>: <ul><li><strong>索引失效</strong>：<code>SELECT * FROM user WHERE name LIKE &#39;%name&#39;;</code></li><li><strong>索引有效</strong>：<code>SELECT * FROM user WHERE name LIKE &#39;name%&#39;;</code></li></ul></li></ul></li><li><p><strong>在 <code>WHERE</code> 子句中使用 <code>OR</code> 连接条件</strong></p><ul><li><strong>描述</strong>: 当 <code>OR</code> 条件的一侧是索引列，而另一侧是非索引列时，整个查询的索引将会失效。</li><li><strong>原理</strong>: 优化器认为，既然对非索引列的查询无论如何都需要进行全表扫描，那么不如直接对整个 <code>OR</code> 条件进行全表扫描。</li><li><strong>示例</strong>: 假设 <code>id</code> 是主键索引，<code>age</code> 是非索引列。 <ul><li><strong>索引失效</strong>（全表扫描）：<code>SELECT * FROM user WHERE id = 1 OR age = 18;</code></li></ul></li></ul></li></ol>`,19)]))}const r=s(d,[["render",l]]),c=JSON.parse('{"path":"/notes/interview/gah9scpa/","title":"索引","lang":"zh-CN","frontmatter":{"title":"索引","createTime":"2025/07/24 22:25:07","permalink":"/notes/interview/gah9scpa/"},"readingTime":{"minutes":6.48,"words":1945},"git":{"createdTime":1753699516000,"updatedTime":1753699516000,"contributors":[{"name":"dead_summer","username":"","email":"2941325451@qq.com","commits":1,"avatar":"https://gravatar.com/avatar/d7f172441d823f01ad688f425860dbe76a31ea11c9936176bbd8c14670a619f8?d=retro"}]},"filePathRelative":"notes/八股文自救指南/MySQL/索引.md","headers":[]}');export{r as comp,c as data};
