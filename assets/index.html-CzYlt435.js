import{a as r,c as t,b as s,o as n}from"./app-CLHFgJWN.js";const o={};function a(i,e){return n(),t("div",null,e[0]||(e[0]=[s('<h2 id="https" tabindex="-1"><a class="header-anchor" href="#https"><span>HTTPS</span></a></h2><h3 id="https-的加解密过程是怎样的" tabindex="-1"><a class="header-anchor" href="#https-的加解密过程是怎样的"><span>HTTPS 的加解密过程是怎样的？</span></a></h3><p>HTTPS (Hypertext Transfer Protocol Secure) 的核心是在 HTTP 的基础上，通过 SSL/TLS (Secure Sockets Layer/Transport Layer Security) 协议对通信内容进行加密，以保证数据传输的安全性、完整性和身份认证。</p><p>其加解密过程主要分为两个阶段：<strong>握手阶段</strong>和<strong>数据传输阶段</strong>。</p><h4 id="_1-握手阶段-handshake-非对称加密协商对称密钥" tabindex="-1"><a class="header-anchor" href="#_1-握手阶段-handshake-非对称加密协商对称密钥"><span>1. 握手阶段 (Handshake) - 非对称加密协商对称密钥</span></a></h4><p>握手阶段的目标是，在客户端和服务器之间安全地协商出一套用于后续数据传输的<strong>对称加密密钥</strong>。这个过程主要使用<strong>非对称加密</strong>来保证密钥协商的安全性。</p><p>具体步骤如下：</p><ol><li><p><strong>Client Hello</strong>：客户端向服务器发起请求，并发送以下信息：</p><ul><li><p>客户端支持的 SSL/TLS 协议版本。</p></li><li><p>一个客户端生成的<strong>随机数 (Random 1)</strong>。</p></li><li><p>客户端支持的加密套件列表 (Cipher Suites)，包括加密算法、哈希算法等。</p></li></ul></li><li><p><strong>Server Hello</strong>：服务器收到请求后，从中选择一套加密套件，并向客户端返回以下信息：</p><ul><li><p>确认使用的 SSL/TLS 协议版本。</p></li><li><p>一个服务器生成的<strong>随机数 (Random 2)</strong>。</p></li><li><p>确认使用的加密套件。</p></li><li><p><strong>服务器的数字证书</strong> (其中包含了服务器的<strong>公钥</strong>)。</p></li></ul></li><li><p><strong>客户端验证与密钥生成</strong>：</p><ul><li><p>客户端收到服务器的响应后，首先会验证服务器数字证书的合法性（由受信任的 CA 机构颁发、未过期、域名匹配等）。</p></li><li><p>如果证书验证通过，客户端会再生成一个<strong>随机数 (Pre-master Secret)</strong>。</p></li><li><p>客户端使用从证书中获取的<strong>服务器公钥</strong>，对这个 <code>Pre-master Secret</code> 进行加密。</p></li></ul></li><li><p><strong>Client Key Exchange</strong>：客户端将加密后的 <code>Pre-master Secret</code> 发送给服务器。</p></li><li><p><strong>服务器解密与密钥生成</strong>：</p><ul><li><p>服务器收到加密的 <code>Pre-master Secret</code> 后，使用自己的<strong>私钥</strong>进行解密，得到原始的 <code>Pre-master Secret</code>。</p></li><li><p>至此，客户端和服务器都拥有了相同的三个随机数：<code>Random 1</code>、<code>Random 2</code> 和 <code>Pre-master Secret</code>。</p></li></ul></li><li><p><strong>生成会话密钥</strong>：客户端和服务器使用相同的算法，通过这三个随机数，各自独立地生成一个完全相同的<strong>会话密钥 (Session Key)</strong>。这个密钥就是后续用于<strong>对称加密</strong>的密钥。</p></li><li><p><strong>Finished</strong>：双方各自发送一个 <code>Finished</code> 消息，该消息使用刚刚生成的会话密钥进行加密，以验证握手过程是否成功且未被篡改。如果双方都能正确解密对方的 <code>Finished</code> 消息，则握手完成。</p></li></ol><h4 id="_2-数据传输阶段-对称加密" tabindex="-1"><a class="header-anchor" href="#_2-数据传输阶段-对称加密"><span>2. 数据传输阶段 - 对称加密</span></a></h4><p>握手成功后，客户端和服务器之间已经建立了一个安全的通道。</p><ul><li><p><strong>加密</strong>：发送方（无论是客户端还是服务器）在发送应用层数据（如 HTTP 请求/响应）之前，会使用之前协商好的<strong>会话密钥</strong>和<strong>对称加密算法</strong>（如 AES）对数据进行加密。</p></li><li><p><strong>解密</strong>：接收方收到加密数据后，使用相同的<strong>会话密钥</strong>和算法进行解密，得到原始数据。</p></li></ul><h4 id="总结" tabindex="-1"><a class="header-anchor" href="#总结"><span>总结</span></a></h4><ul><li><p><strong>非对称加密</strong>：用于握手阶段，解决了<strong>密钥交换</strong>的问题。它的计算开销大，速度慢，不适合用于加密大量业务数据。</p></li><li><p><strong>对称加密</strong>：用于数据传输阶段，解决了<strong>通信效率</strong>的问题。它的加解密速度快，开销小，适合加密业务报文。</p></li><li><p><strong>数字证书</strong>：用于<strong>身份认证</strong>，解决了确认服务器身份、防止中间人攻击的问题。</p></li></ul><p>通过这种混合加密机制，HTTPS 兼顾了安全性和性能，实现了端到端的数据加密传输。</p>',14)]))}const p=r(o,[["render",a]]),g=JSON.parse('{"path":"/notes/interview/ssw59qes/","title":"应用层","lang":"zh-CN","frontmatter":{"title":"应用层","createTime":"2025/07/24 22:41:44","permalink":"/notes/interview/ssw59qes/"},"readingTime":{"minutes":3.19,"words":958},"git":{"createdTime":1753699516000,"updatedTime":1753699516000,"contributors":[{"name":"dead_summer","username":"","email":"2941325451@qq.com","commits":1,"avatar":"https://gravatar.com/avatar/d7f172441d823f01ad688f425860dbe76a31ea11c9936176bbd8c14670a619f8?d=retro"}]},"filePathRelative":"notes/八股文自救指南/计算机网络/应用层.md","headers":[]}');export{p as comp,g as data};
